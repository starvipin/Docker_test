[project]
name = "docker-test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "flask>=3.1.3",
    "pandas>=3.0.2",
    "pytest>=9.0.3",
]
